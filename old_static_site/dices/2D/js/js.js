//jQuery(document).ready(function($){
// var randomPoints = getRandomPoints(20),
    // pointsOnCircle = getPointsOnCircle(randomPoints, circleOpts);

// drawCircle(circleOpts);
// drawPointsBig(randomPoints);
// drawLinesToCircle(randomPoints, circleOpts);
// console.info(pointsOnCircle);
// printObj(pointsOnCircle);

// doBalancePoints(pointsOnCircle);
//});

// drawNewScene(43);