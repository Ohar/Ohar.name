{
  "name": "ohar.name",
  "version": "0.0.5",
  "description": "Site ohar.name",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf ./dist/*",
    "deploy": "sh ./shell_scripts/deploy.sh",
    "build": "npm run clean && bestzip ./dist/ohar.name.zip old_static_site/*",
    "release": "npm version patch && npm run build && npm run deploy && git push origin --tags && git push"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Ohar/Ohar.name.git"
  },
  "author": "Pavel Lysenko aka Ohar",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Ohar/Ohar.name/issues"
  },
  "homepage": "https://github.com/Ohar/Ohar.name#readme",
  "devDependencies": {
    "bestzip": "^1.1.4",
    "rimraf": "^2.6.2"
  }
}
